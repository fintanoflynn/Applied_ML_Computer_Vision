# Applied_ML_Computer_Vision
Computer vision model to detect leaf disease. 

# Installing packages
Use "uv add package-name" to install packages or "uv remove package-name"
example: uv add kagglehub

